# ML-DL_1st_cls_model
This project implements Linear Regression using NumPy and PyTorch. NumPy part builds gradient descent manually, while PyTorch uses an nn.Module model, loss function, and optimizer. The project generates loss curves and regression line plots for training and testing.
